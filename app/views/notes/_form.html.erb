<%=form_with(model: note) do |f|%>
  <%= f.label :title%>
  <%= f.text_field :title%>

  <%= f.label :content%>
  <%= f.rich_text_area :content%>

  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit :submit%>
<% end %>
